### host_file_export
Export a Vini AI workspace file back to a scoped Windows host path.
This is approval-gated by the desktop app. Use after saving Office work that should return to Windows.

Args: `local_path`, `host_path`
