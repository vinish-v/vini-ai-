### host_file_list
List files in a scoped Windows host folder through the Vini AI host bridge.
Use only for folders returned by host bridge status or clearly inside approved scopes.
Never claim full-disk access.

Args: `path`
