### host_file_open_app
Open a scoped Windows host file with the Windows default application.
This opens on the user's Windows desktop, not inside Vini's in-app Xpra Desktop, and requires approval.

Args: `path`
